int * pointer_to_a = &a
* means you are declaring a pointer. It also means you are to search for the value where the pointer leads instead of using the memory address -) *pointer_to_a = 1 vs pointer_to_a = 1
& means you are refering to the memory adress where a remains
